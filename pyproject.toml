[project]
name = "pentimento"
version = "0.1.0"
description = "Status, intent, and lineage over agent plan files."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []

[project.scripts]
pentimento = "pentimento.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["pentimento*"]

[tool.ruff]
line-length = 100
