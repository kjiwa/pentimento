## Checklist

- [ ] `python3 -m unittest discover` passes
- [ ] `uvx ruff check` and `uvx ruff format --check` pass
- [ ] `sh demo/capture.sh` re-run if this PR could change CLI output, and `README.md` diff reviewed
